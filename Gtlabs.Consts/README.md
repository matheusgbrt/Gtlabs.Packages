# Gtlabs.Consts

Shared constants and configuration models used by GT Labs packages.

Includes common header names, configuration field names, JWT token claim names, token type constants, and JWT configuration models.

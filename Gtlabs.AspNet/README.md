# Gtlabs.AspNet

Convenience registration package for common ASP.NET Core service setup used by GT Labs applications.

It wires shared features such as ambient data, API call helpers, dependency registration, action filters, JSON options, and JWT configuration binding.

## Usage

```csharp
services.AddBasicFeatures(configuration);
```
